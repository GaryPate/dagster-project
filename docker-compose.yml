services:

  dagster-webserver:
    build:
      context: .
      dockerfile: Dockerfile
    image: australia-southeast1-docker.pkg.dev/ml-dev-403200/dagster-docker/dagster-project
    ports:
      - 3000:3000

  dagster-daemon:
    build:
      context: .
      dockerfile: Dockerfile
    image: australia-southeast1-docker.pkg.dev/ml-dev-403200/dagster-docker/dagster-project
    command: "dagster-daemon run"
    # environment:
    #   - PGPASS=${PGPASS}
    #   - PGUID=${PGUID}

#   dagster-postgres:
#     image: postgres:13.3
#     ports:
#       - 5432:5432
#     environment:
#       - POSTGRES_PASSWORD=${PGPASS}
#     volumes:
#       - dagster-postgres:/var/lib/postgresql/data
# volumes:
#   dagster-postgres:
#     driver: local